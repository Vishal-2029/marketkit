import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (tester) async {
    // Placeholder — add real tests as screens are built
    expect(true, isTrue);
  });
}
