[build]
  cmd = "go build -o ./tmp/server ./cmd/api"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "uploads"]
  delay = 1000

[log]
  time = true

[color]
  main     = "magenta"
  watcher  = "cyan"
  build    = "yellow"
  runner   = "green"
